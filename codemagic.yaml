workflows:
  android-workflow:
    name: Android Workflow
    max_build_duration: 60
    scripts:
      - name: Build APK
        script: |
          unzip shah-auto-deals-fixed.zip -d project
          cd project/shah-auto-deals
          chmod +x gradlew
          ./gradlew assembleDebug
    artifacts:
      - project/shah-auto-deals/app/build/outputs/**/*.apk
